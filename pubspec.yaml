name: flutter_zeplin
description: control your flutter app on zeplin
version: 0.0.3
homepage: https://github.com/Abdusin/Zeplin_Flutter
repository: https://github.com/Abdusin/Zeplin_Flutter

platforms:
  android:
  ios:
  linux:
  macos:
  windows:

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  code_builder: ^4.1.0
  dart_style: ^2.2.1
  change_case: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
